<%= simple_form_for [@project, @manager] do |f| %>
  <%= f.input :name %>
  <%= f.input :email %>
  <%= f.input :password %>
  <%= f.input :password_confirmation%>
  <%= f.input :is_admin%>
  <%= f.button :submit, class: 'btn-u pull-right' %>
<% end %>
